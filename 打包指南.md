# ASBT 自动存档备份工具 - Windows 打包指南

## 方法一：使用 Python + PyInstaller（推荐）

### 第一步：准备环境

1. **下载并安装 Python 3.8 或更高版本**
   - 官网：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **打开命令提示符 (CMD) 或 PowerShell**

3. **进入应用程序目录**
   ```cmd
   cd [应用程序路径]\pywebapp
   ```

### 第二步：安装依赖

```cmd
pip install webview pyinstaller
```

### 第三步：打包

#### 方式 A：使用打包脚本
```cmd
build.bat
```

#### 方式 B：手动打包
```cmd
:: 清理旧文件
if exist "build" rmdir /s /q build
if exist "dist" rmdir /s /q dist

:: 打包
pyinstaller --onefile --windowed --name "ASBT-自动存档备份工具" --add-data "index.html;." --add-data "renderer.js;." app.py
```

### 第四步：找到生成的 exe 文件

打包成功后，exe 文件位于：
```
dist\ASBT-自动存档备份工具.exe
```

---

## 方法二：使用 Electron 版本

### 第一步：安装 Node.js

1. 下载并安装 Node.js (LTS 版本)：https://nodejs.org/
2. 重启命令行工具

### 第二步：安装依赖

```cmd
cd [应用程序路径]\web-version
npm install
```

### 第三步：打包

```cmd
npm run build
```

### 第四步：找到生成的 exe 文件

打包成功后，exe 文件位于：
```
web-version\dist\win-unpacked\ASBT-自动存档备份工具.exe
```

---

## 常见问题

### Q1: pip install 失败怎么办？

**解决方法：**
```cmd
# 升级 pip
python -m pip install --upgrade pip

# 使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple webview pyinstaller
```

### Q2: 打包后运行报错 "DLL not found"？

**解决方法：**
1. 安装 Visual C++ Redistributable
2. 下载地址：https://aka.ms/vs/17/release/vc_redist.x64.exe

### Q3: webview 需要 GTK 依赖？

**这是 Linux 特定的问题。** 在 Windows 上不需要额外安装 GTK。

### Q4: 如何创建桌面快捷方式？

1. 右键点击 exe 文件
2. 选择 "发送到" → "桌面快捷方式"
3. 或者按住 Ctrl + Shift 然后拖动文件到桌面

### Q5: 打包后的程序如何移动到其他电脑？

**便携版本：** 直接复制 exe 文件和所需的数据文件即可。

**注意：** 如果程序使用了相对路径引用 index.html 和 renderer.js，打包后的 exe 需要和这些文件在同一目录下。

---

## 完整打包命令示例

```cmd
@echo off
chcp 65001 > nul

echo ========================================
echo   ASBT 自动存档备份工具 - 一键打包
echo ========================================
echo.

:: 检查 Python
python --version > nul 2>&1
if errorlevel 1 (
    echo [错误] 未找到 Python，请先安装！
    pause
    exit
)

:: 安装依赖
echo [步骤 1/3] 安装依赖...
pip install webview pyinstaller -q

:: 清理
echo [步骤 2/3] 清理旧文件...
if exist "build" rmdir /s /q build
if exist "dist" rmdir /s /q dist

:: 打包
echo [步骤 3/3] 开始打包...
pyinstaller --onefile --windowed --name "ASBT-自动存档备份工具" --add-data "index.html;." --add-data "renderer.js;." app.py

echo.
echo ========================================
echo   打包完成！
echo ========================================
echo.
echo exe 文件位置: dist\ASBT-自动存档备份工具.exe
echo.
pause
```

将此代码保存为 `一键打包.bat`，双击运行即可。

---

## 技术支持

如果遇到问题，请检查：
1. Python 是否正确安装（`python --version`）
2. pip 是否可用（`pip --version`）
3. 是否有管理员权限（某些系统需要）
4. 杀毒软件是否拦截（暂时关闭杀毒软件试试）

---

**祝您使用愉快！**
